#!/usr/bin/env bash
# Optionnel : git config core.hooksPath .githooks
# Bloque le push si la non-régression locale échoue (nécessite Netlify lié + secrets locaux).
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
echo "pre-push: npm run regression"
npm run regression
